name: Publish to crates.io

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@nightly
      - uses: Swatinem/rust-cache@v2

      - name: Verify build
        run: cargo build --workspace --all-features

      # Publish cmdreg-macros first (dependency of cmdreg)
      - name: Publish cmdreg-macros
        run: cargo publish -p cmdreg-macros || echo "cmdreg-macros already published, skipping"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      # Wait for crates.io index to update
      - name: Wait for index
        run: sleep 30

      - name: Publish cmdreg
        run: cargo publish -p cmdreg --all-features || echo "cmdreg already published, skipping"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
